<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.idollbom.mapper.applymapper.ClassListMapper">
    <!-- 현재 소카에 대한 COUNT -->
    <select id="classCount">
        select count(*)
        from class
        where class_category_small = '등/하원'
    </select>
    <!-- 소카에 대한 전문가 리스트 가져오는 select -->
    <select id="findAllClass">
        SELECT p.PRO_PROFILE_IMAGE_URL, p.PRO_NAME, C.CLASS_NAME, C.CLASS_CONTENT, P.PRO_ADDRESS
        FROM PRO p JOIN CLASS c
        ON p.PRO_NUMBER = 11
        AND c.CLASS_CATEGORY_SMALL = '등/하원'
    </select>
</mapper>